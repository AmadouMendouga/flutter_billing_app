{
  "framework": null,
  "installCommand": "echo 'No install step needed before the Flutter build'",
  "buildCommand": "bash vercel-build.sh",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
